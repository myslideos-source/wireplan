/** Shared drag-and-drop payload type between the toolbar's draggable
 * device chips and the canvas drop target (§3 — "per Drag & Drop direkt
 * auf den Grundriss ziehen"). One constant so the source and target never
 * drift apart. */
export const DRAG_TOOL_MIME = "application/x-wireplan-tool";
